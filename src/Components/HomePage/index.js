import React from 'react';
import { BackgroundImg } from './style'

const HomePage = () => {
    return (
        <div>
            <BackgroundImg/>
        </div>
    );
};

export default HomePage;